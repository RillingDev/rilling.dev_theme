{% for tag in tags %}
    <a class="badge {{dark ? 'badge-secondary' : 'badge-primary'}}" title="Show all articles with tag {{tag}}" href="{{url(page.blog ~ '/tag' ~ config.system.param_sep ~ tag, true)}}">{{tag}}</a>
{% endfor %}

